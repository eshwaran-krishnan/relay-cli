node_modules/
# dist/ is committed — it's the prebuilt single-file bundle that ships
# with the package for direct-from-github installs. Rebuild with `npm run build`.
bun.lock.local
.env
.env.local
.DS_Store
*.log
